import React from "react";

const ReachComponent = ({ data }) => {
  return (
    <div className="channel-reach">
      <h2 className="reach-text">Reach and Engagment</h2>
      <span className="reach-card" />
    </div>
  );
};

export default ReachComponent;
